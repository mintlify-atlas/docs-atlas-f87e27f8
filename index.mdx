---
title: "eXeLearning for Moodle: Embed, Edit, and Grade eXeLearning Resources"
sidebarTitle: "Home"
description: "mod_exelearning embeds eXeLearning v4 packages in Moodle with native navigation, per-iDevice gradebook columns, and an in-browser editor."
---

`mod_exelearning` is a Moodle activity module that brings eXeLearning v4 packages (`.elpx`) directly into your Moodle courses. It preserves the package's native sidebar navigation while automatically registering each gradable iDevice as its own column in the Moodle gradebook — so a single resource containing two quizzes produces two independent grade entries. Teachers can edit packages in-place using the embedded eXeLearning editor without ever leaving Moodle.

<CardGroup cols={2}>
  <Card title="Quick Start" icon="rocket" href="/quickstart">
    Try mod_exelearning in your browser with Moodle Playground — no installation needed.
  </Card>
  <Card title="Installation" icon="download" href="/installation">
    Install from a release ZIP or manually into your Moodle instance.
  </Card>
  <Card title="For Teachers" icon="chalkboard-teacher" href="/guides/adding-activity">
    Add eXeLearning resources, edit packages in-place, and manage grades.
  </Card>
  <Card title="API Reference" icon="code" href="/api/overview">
    Web-service API for the Moodle App and external clients.
  </Card>
</CardGroup>

## What makes mod_exelearning different

Unlike a plain SCORM player, `mod_exelearning` keeps the authored navigation sidebar and maps each gradable iDevice to its own gradebook column. It merges the best of its two sibling plugins:

- **`mod_exeweb`** — preserves eXeLearning's native sidebar, but no grading.
- **`mod_exescorm`** — grades activities, but only as a single aggregated SCORM column.

`mod_exelearning` delivers both: native navigation **and** per-iDevice grade columns.

## Key features

<CardGroup cols={2}>
  <Card title="Native Sidebar Navigation" icon="sidebar">
    Students browse eXeLearning content exactly as the author designed it, with the full sidebar intact.
  </Card>
  <Card title="Per-iDevice Gradebook" icon="table-columns">
    Each gradable exercise (true/false, drag-and-drop, crossword…) registers as its own gradebook column.
  </Card>
  <Card title="Embedded Editor" icon="pen-to-square">
    Edit `.elpx` packages directly inside Moodle using the built-in eXeLearning editor — no download required.
  </Card>
  <Card title="Multi-Attempt Support" icon="rotate">
    Track every attempt and aggregate grades by highest, average, first, last, or lowest score.
  </Card>
  <Card title="Moodle App API" icon="mobile">
    Full web-service API registered under MOODLE_OFFICIAL_MOBILE_SERVICE for the official Moodle App.
  </Card>
  <Card title="GDPR Compliant" icon="shield-halved">
    Privacy provider declares all personal data, supports export and deletion with automatic grade recalculation.
  </Card>
</CardGroup>

## Get started in minutes

<Steps>
  <Step title="Try without installing">
    Click the [Moodle Playground badge](https://moodle-playground.com/?blueprint-url=https://raw.githubusercontent.com/exelearning/mod_exelearning/main/blueprint.json) to get a fully configured Moodle instance in your browser with `mod_exelearning` already installed and a demo course ready to explore.
  </Step>
  <Step title="Download and install">
    Download the latest release ZIP from [GitHub Releases](https://github.com/exelearning/mod_exelearning/releases) and upload it via _Site administration → Plugins → Install plugins_. See the [Installation guide](/installation) for full details.
  </Step>
  <Step title="Add an activity">
    In your course, turn editing on, choose **Add an activity or resource**, and select **eXeLearning resource**. Upload your `.elpx` file and the gradebook columns are created automatically.
  </Step>
  <Step title="Install the embedded editor">
    From _Site administration → Plugins → Activity modules → eXeLearning resource_, use the **Manage embedded editor** widget to download and install the editor directly from GitHub. See [Embedded Editor Management](/admin/embedded-editor-management).
  </Step>
</Steps>

## Compatibility

`mod_exelearning` supports **Moodle 4.5 LTS** through **Moodle 5.2** and requires **PHP 8.1+**. See the full [compatibility matrix](/compatibility).
